import React, { use } from 'react';

const CategoryLayout = ({ products }) => {
    const productLists = use(products);
    // console.log(productLists, typeof productLists, Array.isArray(productLists));
    return (
        <div>

        </div>
    );
};

export default CategoryLayout;